# Community Agents Example

Mock/devnet acceptance example for the DNA x402 Community Agent Pack.

It loads every template under `templates/agents/community`, validates Telegram/Discord support metadata, checks receipt and fee behavior, and simulates a receipt-gated unlock path.

```bash
npm run examples:community
```

No wallet, private key, bot token, or live payment is required.
