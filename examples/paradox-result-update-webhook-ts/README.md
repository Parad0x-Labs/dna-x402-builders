# Parad0x Result Update Webhook Example

Shows how a builder can meter result or settlement updates for Parad0x-powered signals.

```bash
npm run examples:paradox-result-webhook
```

The example does not use real webhook secrets.
