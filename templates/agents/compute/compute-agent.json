{
  "name": "Compute Agent",
  "slug": "compute-agent",
  "pack": "core",
  "category": "compute",
  "description": "Sell inference, analysis, scraping, simulation, or compute jobs through receipt-gated execution.",
  "degenPitch": "Meter compute, inference, and jobs with receipts instead of trust.",
  "whatItDoes": [
    "Quotes a compute job before queueing.",
    "Runs the job after receipt verification.",
    "Returns job status, output URL, and receipt ID."
  ],
  "publicBetaMode": "low_risk_capped_live_payments",
  "defaultMode": "USER_CONFIRMED_LIVE",
  "walletModel": "NONE_REQUIRED",
  "backendCustody": false,
  "backendSigning": false,
  "riskLimits": {
    "maxBetUsd": 200,
    "maxDailySpendUsd": 1500,
    "maxDailyLossUsd": 300,
    "maxOpenExposureUsd": 500
  },
  "copyRules": {
    "copyBuys": false,
    "copySells": false,
    "copyExits": false,
    "minEntryPriceBps": null,
    "maxEntryPriceBps": null
  },
  "monetization": {
    "enabled": true,
    "builderFeeBps": 100,
    "alphaFeeBps": null,
    "appliesTo": "PROOF_REVIEWED_ACTION"
  },
  "receiptBehavior": {
    "receiptRequired": true,
    "bindsProof": true,
    "bindsFeeWaterfall": true
  },
  "notInBetaScope": [
    "Unsafe scraping targets",
    "Private credential handling",
    "Backend signing",
    "Hidden fees"
  ],
  "cursorPrompt": "Build a paid compute job endpoint using DNA x402. Quote the job, commit, finalize payment, verify the receipt, enqueue the job, and return job status plus receipt ID. Keep secrets out of the repo and default to mock execution.",
  "suggestedPrompt": "Build a paid compute job endpoint using DNA x402. Quote the job, commit, finalize payment, verify the receipt, enqueue the job, and return job status plus receipt ID. Keep secrets out of the repo and default to mock execution.",
  "exampleFlow": [
    "POST /v1/quotes",
    "POST /v1/commits",
    "POST /v1/finalize",
    "POST /v1/paid-retry"
  ],
  "exampleApiFlow": [
    "POST /v1/quotes",
    "POST /v1/commits",
    "POST /v1/finalize",
    "POST /v1/paid-retry"
  ],
  "receiptProofBehavior": [
    "Receipt gates queue admission.",
    "Receipt ID follows the job through status and result endpoints.",
    "Paid retry returns the existing job instead of double-charging."
  ]
}
